<form method="post" action="{{ path('app_bras_delete', {'id': bra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bra.id) }}">
    <button class="btn">Delete</button>
</form>
