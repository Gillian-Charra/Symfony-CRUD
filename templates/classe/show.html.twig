{% extends 'base.html.twig' %}
{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ classe.nom }}</td>
            </tr>
            <tr>
                <th>Base_ATT</th>
                <td>{{ classe.baseATT }}</td>
            </tr>
            <tr>
                <th>Base_DEF</th>
                <td>{{ classe.baseDEF }}</td>
            </tr>
            <tr>
                <th>Base_PV</th>
                <td>{{ classe.basePV }}</td>
            </tr>
            <tr>
                <th>Base_VIT</th>
                <td>{{ classe.baseVIT }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_index') }}">back to list</a>

    <a href="{{ path('app_classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
