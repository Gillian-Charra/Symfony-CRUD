<form method="post" action="{{ path('app_jambes_delete', {'id': jambe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jambe.id) }}">
    <button class="btn">Delete</button>
</form>
