<form method="post" action="{{ path('app_monture_delete', {'id': monture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monture.id) }}">
    <button class="btn">Delete</button>
</form>
