{% extends 'base.html.twig' %}
{% block body %}
    <h1>Edit Heros</h1>

    {{ include('heros/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_heros_index') }}">back to list</a>

    {{ include('heros/_delete_form.html.twig') }}
{% endblock %}
